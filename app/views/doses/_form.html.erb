<div class="col-sm-6">
  <div class="form left">
    <h4>Add Ingredients</h4>
      <%= simple_form_for [@cocktail, @dose] do |f| %>
      <%= f.input :ingredient_id, collection: Ingredient.all %>
      <%= f.input :quantitée, placeholder: 'How much ?' %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
<div class="right">  </div>
